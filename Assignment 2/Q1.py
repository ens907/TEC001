length = float(input("Enter the length of the zander:"))
difference = 42 - length

if length < 42:
    print("Release the fish back into the lake. It is", difference, "cm below the limit.")
else:
    print("You can keep the fish.")
